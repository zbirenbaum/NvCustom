require("marks").setup({
  mappings = {
    toggle = "mm",
    set_next = "m,",
    next = "mn",
    preview = "m;",
    set_bookmark0 = "m0",
    prev = "mp", -- pass false to disable only this default mapping
  },
})
